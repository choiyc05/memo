# 2026-04-17 전체 회의


- **1교시**
1) DB / 파일 관리 (코드 외 업로드)
```
1. 파일 통합 관리 
2. 파일용 서버
3. 팀마다 관리할 것인가.
=> 파일용DB통합
```

2) Error 처리 방식
- Error별 Page 생성 (front)
```
1. 예외 처리 404
2. 접근 권한 401, 403
3. 서버 점검 503
```
- back 기준 Error 코드
```
로그 파일 생성(e)
=> 팀별로 서버에 관리 (파일 저장)
```

```bash
- 각 서비스별 결정 사항
로그 파일 저장 방식?
볼륨 내에? 외에? 저장 이름?
```

3) 로그 모니터링
- 로깅 포멧
```
front : 관리자(front)화면에 출력
=> 실제 로그 파일 저장 형태를 보고 결정


(공통 부분: 로그인, 파일 업로드)
=> [회의록](https://docs.google.com/document/d/1-sMJf3WGg11gYiew3TT7pMO3CzTGwXsJZ2pREeRKpyo/edit?tab=t.bp5txd1atnb7) 2026-04-17 탭 기준 통일 / 추가 issue시 수정
```

```bash
=> 5교시에 테스트 후 fix
```

4) DB 접근 규칙
- ORM / SQL문
```
=> ORM X , SQL만
폴더 내 파일(**.sql)로 관리
```
- DB 수정 시
```
일정 기간동안 원본+수정 저장 관리.
특정 시점(회의) 후 삭제or변경 확정
```

```bash
어느 정도 수준의 증빙자료가 필요한지?
```

---

- **2교시**
1) DB 접근 규칙
- table, column, PK, FK 명명 규칙
```
table, column : snake_name
PK : id 고정?
FK : {table}_id 고정?
=> 
```

- PK 보안?
```
DB에서 AUTO_INCREMENT는 무조건 정수 순서대로 생성
난수로 PK (uuid 등) 지정 시 python 등에서 생성해서 DB 입력 필요.
1. 그냥 AUTO_INCREMENT, 2. UUIDv4, 3. UUIDv7, 4. ULID
=> 의미없고 AUTO_INCREMENT
```

- 함수 Procedure
```bash
공통으로 필요할 부분?
계정 정보, 사업자등록증, 소수점 분리 등 계산식 로직, TIMEZONE, 권한 부여, 코드 테이블
=> 함수 만들기는 하되, 후에 만들기로... 날짜는??
```

---

- **3교시**

- 데이터 암호화
```bash
이유) 작년 쿠팡, 통신사들 보안 이슈 -> 대비 필요
주말에 알아오기?
Part.   1. 방법 : 라이브러리 사용 > 예시 및 장단점 찾아오기
        2. 알고리즘 : DES, RSA, AES ...
```
#### **숙제**
- [데이터 암호화 OWASP Top10](https://www.pentasecurity.co.kr/insight/owasp-top-10-2025-rc1-explained/)
```
주말 숙제로 하나씩 알아오기
```

- WBS, 기능명세서, 요구사항, 업무 흐름도, 테이블 정의서, 테스트 시나리오,테스트 결과서

- **4교시**

- 메인 UI 컨셉
```
진입 - 프로젝트 로고 / 설명 짧게 - 로그인 - KPI,진행도...

에러 페이지 - 

테마 컬러 + 로고 - 배경 컬러 
예시) #F4F7F9 :베이지, #FFF5EA :흰계열, #FBFFF4 :흰계열
```

- **5교시**
- 팀이름
```bash
W.I.T.H ( We are IT Hero! )
```
- github issue template
```
feature/bug/hotfix/trouble shooting/feature RQ
PR/Update/refactoring/custom(기타이슈)
```

- **7교시**
- DB 종류?
```
MariaDB / Postgresql
```

- Front CI
```

```
