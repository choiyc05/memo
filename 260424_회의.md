# 260424 회의

## 1,2 교시 전체 회의
- 시스템 아키텍쳐 수정
```
- gateway
=> Java spring cloud gateway
기대 효과 : 로그 한 곳에서 수집 가능 /
```

- DB 관련
```
월요일 회의 예정?
company_company table 변경
로그인 시 정보가 많아질 수 있다.

협력사 추가 시 가입된 회원이면 꼭 이메일을 보내야 하는가?
웹소켓 이용해서 알림
```

- 서버 PC
```
개발 메인 서버 192.168.0.205
AI 돌릴 pc 테스트 
```


- 협력사 초대장 (신규 회원이라 회원 가입 필요 시) : 트러블 슈팅
```
협력사가 미가입 상태라 초대장 메일 통해 가입 후 자동 협력 관계 추가 로직을 만들 것인가?
=> `링크 타고 들어가야만` 자동 추가가 가능하지만, 현재 기업 회원 가입 시 사업자번호, 사본(사진) 등으로 바로 가입하지 않을 확률 높음(human error).
자동 로직 만들 필요성 떨어짐.

```